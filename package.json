{
  "name": "queue-service",
  "version": "0.1.0",
  "license": "MIT",
  "author": "Ben Isenstein",
  "files": [
    "src"
  ],
  "scripts": {
    "watch": "bun --watch run src/index.ts",
    "prestart": "if [ ! -d \"./sqlite\" ]; then mkdir \"./sqlite\"; fi",
    "start": "bun run src/index.ts"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "dependencies": {},
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
