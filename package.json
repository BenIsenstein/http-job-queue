{
  "name": "queue-service",
  "version": "0.1.0",
  "license": "MIT",
  "author": "Ben Isenstein",
  "files": [
    "src"
  ],
  "scripts": {
    "watch": "bun run create-sql-dir && bun --watch run src/index.ts",
    "create-sql-dir": "if [ ! -d \"./sqlite\" ]; then mkdir \"./sqlite\"; fi",
    "start": "bun run src/index.ts",
    "loadtest": "rm -rf sqlite && bun run prestart && bun run src/loadtest.ts"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "dependencies": {},
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
